import { act, renderHook } from "@testing-library/react-hooks";
import { useItemsFacade } from "./facade";

describe("Items facade", () => {
  /*it("should expose items with an empty array as default value", () => {
    const { result } = renderHook(() => useItemsFacade());

    const { items } = result.current;

    expect(items).toStrictEqual([]);
  });

  it("should expose a way to fetch items and set them in the items property", () => {
    const { result } = renderHook(() => useItemsFacade());

    const { items, getItems } = result.current;

    act(() => {
      getItems();
    });

    expect(items).toStrictEqual([]);
  });*/
  it("bla", () => {});
});
